# Required at runtime: public base URL of the Vercel Blob store.
# Example: https://abcd1234.public.blob.vercel-storage.com
BLOB_PUBLIC_BASE=

# Required only for the one-shot upload script (scripts/upload-blobs.ts).
# Vercel project → Storage → Blob → tokens.
BLOB_READ_WRITE_TOKEN=
